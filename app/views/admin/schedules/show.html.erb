<p id="notice"><%= notice %></p>

<h1>上映情報更新</h1>

<%= render 'form', schedule: @schedule, is_readonly: true %>

<%= link_to 'Show', [:admin, @schedule] %> |
<%= link_to 'Back', admin_schedules_path %>
